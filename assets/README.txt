BPR107a-round160.xlsm  — the CURRENT ticket workbook as the user revised it on 2026-09-25.
                         M32 (Recipe Name), AZ32 (Driver Name) and AZ34 (Order No) have no
                         formula; K21 Batch End Time is writable; the Batch Time block is gone
                         from sheets 1-10; sheet "10" exists. This is the file
                         backend/src/lib/mixtrackWorkbook.js and the print agent are written
                         against. COPY THIS to the plant PC and point the print agent's
                         templatePath at it.

BPR107a-unlocked.xlsm  — the earlier licence-free copy from Round 150. Reference only; its cell
                         map is out of date. Do not build against it.
